(function() {

    var type;

    function filterButton(type) {
        if (type=="design") {
            let result = 1;
            return result;
        }

        else if (type=="development") {
            let result = 0;
            return result;
        }

        else if(type=="gallery") {
            console.log("from index");
        }
    }

})();